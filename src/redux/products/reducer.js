export const PRODUCTS_INTIAL_STATE = {
  productsList: [],
  productsMap: {},
};

export const reducer = (state = PRODUCTS_INTIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
